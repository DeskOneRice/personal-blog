/**
 * Astro Content Collections 配置
 *
 * 本项目使用数据库驱动（非文件驱动），所以内容集合为空。
 * 这个文件的存在是为了让 Astro 的内容系统正常初始化。
 */
import { defineCollection } from 'astro:content';

// 预留：将来如果需要混合模式（部分静态内容），可在这里定义集合
export const collections = {};
