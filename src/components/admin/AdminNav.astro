---
/**
 * AdminNav.astro — 管理后台侧边栏导航（分组）
 */
const currentPath = Astro.url.pathname;

const groups = [
  {
    label: '',
    links: [
      { href: '/admin/dashboard', label: '📊 仪表盘' },
    ],
  },
  {
    label: '内容管理',
    links: [
      { href: '/admin/posts', label: '📝 文章管理' },
      { href: '/admin/columns', label: '📚 专栏管理' },
      { href: '/admin/categories', label: '📁 分类管理' },
      { href: '/admin/tags', label: '🏷️ 标签管理' },
    ],
  },
  {
    label: '页面设置',
    links: [
      { href: '/admin/about', label: '👤 关于页' },
      { href: '/admin/quotes', label: '💬 每日一句' },
      { href: '/admin/announcements', label: '📢 公告管理' },
    ],
  },
  {
    label: '其他',
    links: [
      { href: '/admin/friends', label: '🔗 友链管理' },
    ],
  },
];

function isActive(href: string): boolean {
  if (href === '/admin/dashboard') return currentPath === '/admin/dashboard';
  return currentPath.startsWith(href);
}
---

<nav class="space-y-4">
  {groups.map(group => (
    <div>
      {group.label && (
        <p class="px-3 text-xs font-medium text-[var(--color-text-muted)] uppercase tracking-wide mb-1">
          {group.label}
        </p>
      )}
      <div class="space-y-0.5">
        {group.links.map(link => (
          <a
            href={link.href}
            class={`block px-3 py-2 rounded-md text-sm transition-colors ${
              isActive(link.href)
                ? 'bg-[var(--color-accent)]/10 text-[var(--color-accent)] font-medium'
                : 'text-[var(--color-text-secondary)] hover:text-[var(--color-text-primary)] hover:bg-[var(--color-bg-secondary)]'
            }`}
            aria-current={isActive(link.href) ? 'page' : undefined}
          >
            {link.label}
          </a>
        ))}
      </div>
    </div>
  ))}
</nav>
